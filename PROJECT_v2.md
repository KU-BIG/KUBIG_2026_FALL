# 정보 불균형과 Modality Gap: 양방향 조작 실험

> Schrodi et al.(ICLR 2025)의 정보 불균형 이론을, **이미지와 텍스트를 모두 조작하는 2차원 격자**로 확장·검증한다.

---

## 0. 이 문서를 읽는 Claude Code에게

이 문서는 프로젝트 착수 시점의 유일한 컨텍스트다. 아래를 지켜라.

1. **Phase 0(재현)과 Phase 1(파일럿)을 건너뛰지 마라.** 특히 Phase 1은 이 프로젝트 전체의 전제를 검증한다. 여기서 U자 곡선이 안 나오면 설계를 다시 짜야 하므로, 결과가 나오면 **반드시 멈추고 보고**하라.
2. **학습은 하지 않는다.** 전부 frozen 모델에서 임베딩만 뽑는 사후 분석이다.
3. **결과를 예단하지 마라.** 가설이 반증되는 경우도 유효한 결과다(§9). 원하는 그림이 나오도록 파라미터를 고르지 마라.
4. **임베딩은 반드시 캐싱하라.** 격자 실험이라 재추출 비용이 크다.
5. 막히면 멈추고 물어라. 특히 데이터 접근, 라이선스, 이미지 링크 문제는 임의 우회 금지.

---

## 1. 배경

### 1.1 modality gap이란

CLIP은 이미지 인코더와 텍스트 인코더 **두 개를 독립적으로** 두고, 짝지어진 이미지-캡션은 가깝게 나머지는 멀게 만드는 contrastive 학습을 한다. 두 출력은 하나의 공유 임베딩 공간에 놓인다.

그런데 학습이 끝난 공간을 보면 **이미지 무리와 텍스트 무리가 완전히 분리된 두 영역에 놓여 있다**. 짝짓기는 성공했는데 두 무리가 통째로 떨어져 있다. 이것이 modality gap이다.

- CLIP 논문: https://arxiv.org/abs/2103.00020

### 1.2 Liang et al. (NeurIPS 2022) — 구조적 설명

- 논문: https://openreview.net/pdf?id=S7Evzt9uit3
- 코드: https://github.com/Weixin-Liang/Modality-Gap

**지표 L2M을 정의했다:**

```
Δ_gap = mean(L2 정규화된 이미지 임베딩) - mean(L2 정규화된 텍스트 임베딩)
L2M   = ||Δ_gap||
```

개별 임베딩을 **먼저 정규화한 뒤 평균**낸다. 순서가 중요하다. CLIP 기준값 **0.82**.

**주장:** 딥넷은 랜덤 초기화 상태에서도 임베딩을 좁은 원뿔(cone)에 가둔다. 데이터 없이 랜덤 노이즈를 넣어도 나타나는 아키텍처 고유 성질이다. 인코더가 둘이니 서로 다른 원뿔이 생기고, 그래서 **학습 전부터 gap이 존재**한다. contrastive loss는 낮은 온도에서 그 분리를 유지한다.

**핵심 실험 — embedding shift:**

```
x_shift = Normalize(x - λ·Δ_gap)
y_shift = Normalize(y + λ·Δ_gap)
```

양쪽을 반대 방향으로 동시 이동시키고 **재정규화**한다.

### 1.3 Schrodi et al. (ICLR 2025 Oral) — 데이터적 설명 ← 직접적 출발점

- 논문: https://arxiv.org/abs/2404.07983 (v3)
- 코드: https://github.com/lmb-freiburg/two-effects-one-trigger
- 리뷰: https://openreview.net/forum?id=uAFHCZRmXk

**주장: gap의 원인은 이미지와 캡션 사이의 정보 불균형이다.**

메커니즘:

1. 캡션은 손실적이다. 이미지 정보 중 가장 두드러진 것 몇 개만 담는다.
2. **이미지 인코더는 캡션에 뭐가 쓰일지 미리 알 수 없다.** 따라서 이미지의 무엇을 인코딩해야 텍스트와 맞을지 알 수 없다.
3. 최선은 "보통 캡션에 등장하는 것"에 집중하는 것 → **object bias**
4. 동시에 정렬이 원천적으로 제한되므로, 모델은 정렬 대신 **균일성**을 택해 모든 이미지와 모든 텍스트를 서로 멀어지게 만든다 → **modality gap**

두 증상, 하나의 원인. 그래서 제목이 "Two Effects, One Trigger".

**Liang 반박:** gap 없이 초기화해도 gap이 다시 생기고, contrastive loss가 gap을 좁힐 수도 있다. 따라서 cone effect는 주원인이 아니다.

**검증 방식:** MAD(MNIST 기반 합성 데이터)에서 **이미지는 고정하고 캡션의 속성 개수만 조절**. 추가로 CC12M 재학습 시 캡션 조작.

**부수 발견 (우리 실험에 중요):**

| | 내용 |
|---|---|
| Takeaway 1 | 겉보기엔 gap이 클수록 성능이 좋지만 교란 변수(모델·임베딩·데이터셋 크기) 때문. 통제하면 gap이 작을수록 좋다 |
| Takeaway 2 | 소수 차원만 gap을 만든다. 가장 큰 두 차원으로 두 모달리티가 완벽 분리 |
| Takeaway 3 | **사후 조작(차원 제거·평행이동)은 gap을 닫아도 성능을 못 올린다.** 두 모달리티의 이웃 순서가 다르기 때문(정규화 Kendall-τ 거리 ≈ 0.5, 사실상 무작위) |
| Takeaway 4 | object bias는 캡션 등장 빈도 편향이며 속성 과제 성능을 해치지 않는다 |
| 기타 | gap이 로짓의 **엔트로피**와 연결된다 |

**제안한 지표 RMG:** L2M은 (a) 짝이 맞는지 무시하고 (b) 실제 사용 공간 크기를 고려하지 않는다. 그래서:

```
        mean_i d(x_i, y_i)
RMG = ──────────────────────────────────────
      [모달리티 내부 평균 거리] + mean_i d(x_i, y_i)
```

d는 [0,1]로 스케일한 코사인 비유사도. 값은 [0,1].

---

## 2. 문제의식

### 2.1 Schrodi 이론은 한 방향만 전제한다

메커니즘 전체가 **"이미지 정보 >> 캡션 정보"**를 깔고 있다. 사진과 웹 캡션만 보면 당연한 전제다.

**반대 경우는 아무도 보지 않았다.** 미술 해설은 그림에 없는 정보를 담는다. "1520년 로마 약탈 직후 그려졌다", "백합은 성모의 순결을 상징한다" — 픽셀 어디에도 없다.

### 2.2 Schrodi는 텍스트만 조작했다

MAD에서도 CC12M에서도 조작한 것은 캡션이다. **이미지는 항상 고정이었다.** 그래서 조작 축이 하나뿐이고, 불균형을 한쪽 방향으로만 밀 수 있었다.

**이미지도 조작하면 축이 두 개가 되고, 2차원 격자가 만들어진다.** 그리고 격자에는 기존 연구가 도달할 수 없던 영역이 있다 — 텍스트가 우세한 영역.

---

## 3. 연구 질문과 가설

> **RQ1.** gap은 불균형의 **절대량**에 반응하는가, **방향**에 반응하는가?
>
> **RQ2.** 두 모달리티의 정보량이 균형을 이루는 지점이 존재하는가? 존재한다면 그곳에서 gap은 최소가 되는가?
>
> **RQ3.** 같은 균형점에 두 경로(이미지 열화 / 텍스트 절단)로 도달했을 때 결과가 같은가?

### 3.1 핵심 예측 — U자 곡선

Schrodi 이론이 옳다면 다음이 성립해야 한다.

> 이미지 열화 정도를 0에서 점점 키우며 gap을 재면, **어딘가에서 gap이 최소가 되는 지점**이 있어야 한다. 그 지점이 정보 균형점이며, 더 열화시키면 반대 방향 불균형이 생겨 gap이 다시 커져야 한다.

**이것은 강한 예측이며 반증 가능하다.** U자가 아니라 단조 증가면 "열화는 그냥 해로울 뿐"이고, 평탄하면 "gap은 데이터와 무관한 구조적 현상"(Liang 지지)이다.

### 3.2 경쟁 가설

| | 가설 | 예측 |
|---|---|---|
| H1 | 대칭 — 불균형 절대량이 원인 | 방향 무관하게 gap 발생. 균형점에서 최소. U자 |
| H2 | 비대칭 — 메커니즘이 이미지 인코더 특유 | 역방향에서는 gap이 작거나 성질이 다름. 비대칭 U자 |
| H3 | 구조 — gap은 아키텍처 현상 | 조작해도 gap 불변. 평탄한 곡선 (Liang 지지) |

**세 결과 모두 발표 가치가 있다.** §9 참조.

---

## 4. 핵심 설계: 2차원 격자

**이미지 축과 텍스트 축을 각각 조작하여 정보 불균형을 2차원으로 탐색한다.**

```
이미지
정보량
  ↑
  │  ·    ·    ·    ·     ← 원본 이미지
  │
  │  ·    ·    ·    ·
  │            ╱
  │  ·    ·  ╱ ·    ·      ╱ = 균형선 (탐색으로 찾아냄)
  │        ╱
  │  ·  ╱ ·    ·    ·     ← 강하게 열화된 이미지
  └────────────────────→ 텍스트 정보량
     짧음            김
```

- **균형선 아래**: 이미지 우세 — 기존 연구가 다룬 영역
- **균형선 위**: 텍스트 우세 — **미탐색 영역**

### 4.1 텍스트 축

**사다리 A — 정보량(절단).** 동일 텍스트를 길이만 잘라 정보량을 단조 변화시킨다. 완벽한 통제 변수: 내용·출처·문체가 같고 정보량만 변한다.

| 지점 | 토큰 |
|---|---|
| T10 | 10 |
| T20 | 20 (CLIP 실효 한계) |
| T40 | 40 |
| T77 | 77 (CLIP 아키텍처 상한) |
| T150 | 150 (Long-CLIP 필요) |
| T248 | 248 (Long-CLIP 상한) |

> **주의:** CLIP의 명목 상한은 77이지만 **실효 길이는 약 20토큰**이다. 뒤쪽 positional embedding은 학습 중 거의 활성화되지 않아 신뢰하기 어렵다. "77 이내면 안전"이라는 전제는 틀렸다.

**사다리 B — 내용 성격(SemArt 전용).**

| 변이체 | 구성 | 접지성 |
|---|---|---|
| V0 | `a {Type} painting by {Author}, {School} school, {Timeframe}` | 메타데이터 |
| V1 | Title만 | 낮음 |
| V2 | 해설 중 **시각 서술문**만 ("두 인물이 아치형 창가에 서 있다") | 높음 (이미지로 복원 가능) |
| V3 | 해설 중 **해석문**만 ("백합은 순결을 상징한다") | 낮음 (이미지로 복원 불가) |
| V4 | 해설 전문 | 혼합 |

**V2 대 V3가 핵심 비교다.** 둘 다 문장 단위 추출이라 길이가 비슷하므로 길이 교란이 제거되고 내용 성격만 남는다. 77토큰 문제에서도 자유롭다.

문장 분류 절차: 500문장 수동 라벨링 → 기준 확립 → 나머지 자동 분류 → 분류기 정확도 보고. 완벽할 필요 없다. 분류 노이즈는 두 그룹을 섞어 **차이를 줄이는 방향으로만** 작용하므로, 그럼에도 차이가 보이면 보수적 증거다.

### 4.2 이미지 축

**목표는 의미론적 정보를 줄이는 것이지 픽셀 엔트로피를 늘리는 것이 아니다.**

> **중요:** 가우시안 노이즈는 픽셀 엔트로피를 **늘린다**. 의미 정보를 줄이려면 아래 방식이 적절하다. 최소 3종을 비교해 결과가 조작 방식에 좌우되지 않는지 확인할 것.

| 방식 | 파라미터 | 특징 |
|---|---|---|
| 가우시안 블러 | 커널 크기 σ | 고주파 정보 제거. 자연스러움 |
| 다운샘플링 후 업샘플링 | 축소 비율 | 해상도 정보 제거. 정량화 명확 |
| 패치 마스킹 | 마스킹 비율 | MAE 방식. 분포 이동 큼 |
| JPEG 압축 | 품질 계수 | 자연 분포 유지. 실용적 |
| Center crop | crop 비율 | **분포 이동 없음.** 맥락 정보만 제거 |
| 가우시안 노이즈 | σ | 대조군으로만. 정보 감소 아님 |

**권장:** 블러와 다운샘플링을 주력으로, crop을 분포 이동 통제군으로, 가우시안 노이즈를 대조군으로.

### 4.3 격자 크기

**3×3에서 시작하라.** 흥미로운 영역이 확인되면 그 주변만 촘촘하게 채운다. 처음부터 크게 잡으면 계산이 낭비된다.

---

## 5. 데이터셋

### 5.1 MS-COCO — 주 무대

- https://cocodataset.org/

**격자 실험의 중심 데이터다.** 이유:

- Liang의 L2M = 0.82 기준값이 여기서 나왔다 (검산 가능)
- 순방향 불균형의 표준 사례
- 노이즈 축을 쓰면 **SemArt 없이도 텍스트 우세 영역에 도달할 수 있다**

### 5.2 SemArt — 자연 발생 역방향 사례

- 프로젝트: https://noagarcia.github.io/SemArt/
- 다운로드(DOI): https://doi.org/10.17036/researchdata.aston.ac.uk.00000380
- 코드: https://github.com/noagarcia/SemArt
- 논문: https://noagarcia.github.io/docs/VISART2018.pdf

| 항목 | 내용 |
|---|---|
| 규모 | 21,384점 (train 19,244 / val 1,069 / test 1,069) |
| 모달리티 | 유럽 고전 회화 ↔ 예술 해설(artistic comment) |
| 페어링 | 1:1 |
| 속성 | Author, Title, Date, Technique, Type, School, Timeframe |
| 내장 벤치마크 | Text2Art challenge (양방향 retrieval) |
| 이미지 출처 | Web Gallery of Art |

**역할:** 인위적 조작 없이 **자연적으로 텍스트가 우세한 지점**. 격자에서 인공적으로 만든 균형점과 비교하는 검증 사례. 그리고 사다리 B(내용 성격 조작)가 가능한 유일한 데이터.

**확인 필요 (임의 판단 금지):**

- [ ] DOI 페이지 라이선스 문구
- [ ] Web Gallery of Art 이용 약관
- [ ] **이미지 링크 생존율** (2018년 데이터셋, 죽은 링크 가능성)
- [ ] 죽은 링크가 많으면 대안 논의 (https://github.com/georgeblck/art-datasets)

### 5.3 CUB-200-2011 — 선택

- https://data.caltech.edu/records/65de6-vp158

극단적으로 지시적인 캡션(부위별 색·형태 서술). 스펙트럼 왼쪽 끝 보강용.

---

## 6. 인코더

| 구분 | 모델 | 근거 |
|---|---|---|
| 기본 | OpenCLIP ViT-B/16, `pretrained='openai'` | Schrodi가 CLIP ViT-B/16으로 심층 분석. Long-CLIP도 OpenAI CLIP 기반이라 비교 가능 |
| 비교 | ViT-B/32, ViT-L/14 | 모델 크기 효과 |
| 비교 | LAION-2B, DataComp 체크포인트 | 사전학습 데이터별 자연 발생 gap 차이 → 관찰 연구 |
| 긴 텍스트 | Long-CLIP | T150·T248 측정용 |

- OpenCLIP: https://github.com/mlfoundations/open_clip
- Long-CLIP: https://github.com/beichenzbc/Long-CLIP
- 체크포인트: https://huggingface.co/BeichenZhang/LongCLIP-L
- transformers 포팅(권장): https://huggingface.co/creative-graphic-design/LongCLIP-B

> **기준선을 `openai`로 맞출 것.** Long-CLIP은 OpenAI CLIP을 파인튜닝한 것이므로, LAION 체크포인트를 기준으로 비교하면 차이가 "긴 텍스트 처리 능력" 때문인지 "사전학습 데이터" 때문인지 구분되지 않는다.

> **Long-CLIP은 별도 트랙.** 원본 CLIP과 직접 비교하면 모델이 달라지는 교란이 생긴다. 주력 격자는 원본 CLIP 고정으로 하고, Long-CLIP은 독립 비교군으로 보고한다.

---

## 7. 측정

### 7.1 임베딩 추출 지점

**최종 projection 출력의 L2 정규화 직후** (512차원 공유 공간).

```python
import torch, torch.nn.functional as F, open_clip

model, _, preprocess = open_clip.create_model_and_transforms(
    'ViT-B-16', pretrained='openai'
)
model.eval()
tokenizer = open_clip.get_tokenizer('ViT-B-16')

with torch.no_grad():
    img_emb = F.normalize(model.encode_image(images), dim=-1)  # (N, 512)
    txt_emb = F.normalize(model.encode_text(tokens),  dim=-1)  # (N, 512)

delta_gap = img_emb.mean(0) - txt_emb.mean(0)
L2M = delta_gap.norm().item()
```

> **흔한 실수:** Liang의 cone effect 실험과 L2M 계산은 **서로 다른 레이어**를 쓴다. cone effect는 ResNet의 경우 마지막 linear 직전, CLIP의 ViT/Text Transformer는 최종 레이어. Δ_gap은 projection 이후 정규화된 공유 공간. 혼동하면 재현이 어긋난다.

### 7.2 보고할 지표 (전부 계산)

1. **L2M** — Liang 기준, 비교 가능성
2. **RMG** — Schrodi가 L2M의 한계를 명시했으므로 필수. L2M만 쓰면 리뷰에서 지적당한다
3. **차원별 기여도** — |평균 차이|를 차원별로 정렬. Takeaway 2와 대화
4. **이웃 순서 불일치** — 정규화 Kendall-τ 거리. Takeaway 3과 대화
5. **쌍별 코사인 유사도 분포** — cone effect 확인
6. **retrieval 성능** — 정보 균형 조작의 검증 장치 (§7.4)

### 7.3 표본 처리

- 원 논문 n = 5,000 → 동일하게 맞춘다
- **N별 L2M 수렴 곡선** (N = 100, 500, 1000, 5000) — 표본 크기 정당화
- 부트스트랩 95% 신뢰구간
- 데이터셋 간 비교 시 **반드시 샘플 수 고정**
- 이미지당 캡션 1개로 통일 (COCO는 5개, CUB은 10개)

### 7.4 정보 불균형을 어떻게 검증하는가 (중요)

**이미지와 텍스트의 절대적 정보량은 같은 단위로 잴 수 없다.** 이것은 이 프로젝트의 근본적 한계이며, 정직하게 인정해야 한다.

대신 **"텍스트를 이미지로부터 얼마나 예측할 수 있는가"**를 잰다. 이것이 Schrodi 메커니즘이 실제로 말하는 양이다("이미지 인코더는 캡션에 뭐가 쓰일지 모른다").

| 방법 | 내용 | 신뢰도 |
|---|---|---|
| **검색 상한** | 같은 갤러리 크기·인코더로 R@1 비교. 낮을수록 이미지→텍스트 예측 불가 | **높음. 주력** |
| 조건부 예측 가능성 | CLIP score 등으로 "이미지가 주어졌을 때 이 문장의 그럴듯함" | 중간. 도메인 편향 주의 |
| 어휘 대리 지표 | 시각 접지 어휘 대 비접지 어휘 비율 | 거칠지만 투명 |
| 상호정보량 추정 | InfoNCE 하한 등 | 낮음. 인코더 품질에 의존. 보조로만 |

**검색 상한을 주력으로 삼아라.** 특히 V2와 V3에서 R@1을 비교하면, 같은 그림·비슷한 길이·다른 접지성이라는 완벽한 통제가 된다. V3의 R@1이 현저히 낮으면 "이미지로부터 복원 불가능한 정보를 담고 있다"는 직접 증거다.

---

## 8. 실행 순서

### Phase 0 — 재현 (필수)

**목표:** COCO에서 L2M = 0.82 확인.

- MS-COCO validation 5,000쌍, 이미지당 캡션 1개
- OpenCLIP ViT-B/16, `pretrained='openai'`

안 나오면 의심할 것: 정규화 순서(개별 정규화 → 평균), 체크포인트, 샘플 수, 전처리.

**이 단계가 통과해야 다음으로 간다.** 없이 가면 이상한 값이 나왔을 때 버그인지 발견인지 구분할 수 없다.

### Phase 1 — U자 곡선 파일럿 ⭐ 최우선

**이 프로젝트 전체의 전제를 검증하는 단계다.**

- COCO 소규모(1,000쌍 정도)
- 텍스트 고정(원본 캡션), **이미지 열화만 스윕**
- 블러 σ를 0부터 강하게까지 5~7단계
- 각 지점에서 L2M, RMG 측정

**확인할 것:**

- U자 곡선이 나타나는가?
- 최소점이 어디인가?
- 최소점에서 gap이 얼마나 줄어드는가? (0에 가까운가, 상당히 남는가)

**결과가 나오면 멈추고 보고하라.**

| 결과 | 의미 | 다음 |
|---|---|---|
| U자 확인 | 전제 성립 | 격자 확장으로 진행 |
| 단조 증가 | 열화는 그냥 해로움 | 조작 방식 재검토(다른 열화 방식 시도) |
| 평탄 | gap은 구조적(H3) | **설계 재검토 필요. 논의할 것** |

### Phase 2 — 격자 실험 (주력)

Phase 1이 통과하면 진행.

- COCO에서 3×3 격자 (이미지 열화 3단계 × 텍스트 절단 3단계)
- 각 지점에서 §7.2의 지표 전부 측정
- 열화 방식 3종 이상으로 반복하여 강건성 확인

**주력 그림:** 2차원 히트맵 또는 등고선. x축 텍스트 정보량, y축 이미지 정보량, 색 L2M/RMG.

**흥미로운 영역이 보이면 그 주변만 촘촘하게 채운다.**

### Phase 3 — 대칭성 검증 (RQ3)

같은 균형점에 **두 경로로 도달**한다.

- 경로 A: 이미지를 열화시켜 도달
- 경로 B: 텍스트를 잘라서 도달

**두 지점의 gap이 같은가?** 같으면 H1(절대량), 다르면 H2(방향 중요).

또한 **Δ_gap 벡터의 방향**을 비교하라. 크기뿐 아니라 방향이 같은지. 코사인 유사도 한 줄이면 계산되는데 아무도 묻지 않은 질문이다.

### Phase 4 — SemArt 검증

- SemArt에서 원본 상태의 L2M·RMG 측정. COCO 0.82와 비교
- 사다리 B(V0~V4) 전체 측정
- **검색 상한 실험**: V2 대 V3의 R@1 비교 → 접지성 차이 검증
- SemArt의 자연 발생 위치가 격자의 어디에 대응하는지 매핑
- SemArt에도 이미지 열화 축을 적용하여 격자 확장

### Phase 5 — 균형점의 성질 (선택)

균형점이 확인되면 거기서만 물을 수 있는 질문들.

- **이웃 구조가 정렬되는가?** Kendall-τ 거리가 줄어드는가. 줄어들면 "정보 균형이 회복되면 평행이동(Liang의 shift)이 작동하기 시작한다"는 조건부 결론
- **성능은 어떻게 되는가?** 균형점에서는 이미지가 이미 망가져 있으므로 **gap은 최소인데 성능은 나쁠 수 있다.** 관찰되면 "gap 축소가 목표가 되어선 안 된다"는 강한 증거이며 Takeaway 1과 정면 충돌 → 논쟁적
- **gap을 만드는 차원이 바뀌는가?** Takeaway 2 확장

### Phase 6 — 외적 타당성 (선택)

CUB 등에 동일 파이프라인 적용. 경향이 도메인을 넘어 유지되는지.

---

## 9. 결과 해석 가이드

| 결과 | 의미 | 가치 |
|---|---|---|
| U자, 대칭 | H1. 불균형 절대량이 원인 | Schrodi 확장. 깔끔 |
| U자, 비대칭 | H2. 메커니즘이 이미지 인코더 특유 | Schrodi 정교화 필요 → 흥미 |
| 평탄 | H3. gap은 구조적 | Liang 지지, Schrodi 반증 → 논쟁적 |
| 균형점에서 gap 잔존 | 두 이론이 각각 일부 설명 | **기여도 분리**. 매우 좋음 |
| 균형점에서 성능 저하 | gap 축소가 목표가 아님 | Takeaway 1과 충돌 → 논쟁적 |

**어떤 결과가 나와도 논지가 선다.** 가설이 틀릴까 걱정하지 말고 정직하게 측정하라.

---

## 10. 예상 반박과 대응

**"노이즈가 정보를 줄인 게 아니라 분포 이동을 일으킨 것 아닌가"**
→ 가장 강력한 반박. 대응: (a) 성능이 유지되는 약한 열화 구간 중심으로 분석, (b) center crop처럼 자연 분포를 유지하는 방식을 통제군으로 포함, (c) 열화 방식 3종 이상에서 결과 일관성 확인.

**"Schrodi가 이미 텍스트를 조작했다"**
→ 맞다. **우리 기여는 텍스트 조작이 아니라 (a) 이미지 축 추가 (b) 두 축의 교차 (c) 균형점 탐색이다.** 논문에서 이를 명확히 구분할 것. "실제 데이터에서 했다", "학습 없이 했다"는 약한 차별점이므로 주장의 기둥으로 삼지 마라.

**"이미지와 텍스트의 정보량을 비교할 수 없지 않나"**
→ 옳다. 그래서 절대 비교를 하지 않는다. **균형점을 미리 정하지 않고 실험으로 찾아낸다.** 검증은 검색 상한으로 한다(§7.4). 한계로 명시할 것.

**"77토큰 잘림 때문에 나온 결과 아닌가"**
→ 텍스트 처리 방식 3종(truncation / 문장 분할 후 평균 / 요약) 비교. V2·V3는 문장 단위라 애초에 짧다. 길이 고정 부분집합에서 재측정.

**"CLIP이 명화를 외우고 있지 않나"**
→ 가능성 있음. 다만 **이 프로젝트에는 유리하다** — 이미지 쪽이 익숙할수록 변수가 텍스트로 깨끗이 격리된다. 대응: 유명 작가 대 무명 작가로 나눠 비교, Discussion에 명시.

**"L2M만 보고 결론 내렸다"**
→ RMG 병행 보고.

---

## 11. 리포지토리 구조 제안

```
.
├── configs/              # 실험 설정 (모델, 데이터, 격자 좌표)
├── data/
│   ├── coco/
│   ├── semart/
│   └── cub/              # 선택
├── src/
│   ├── degrade.py        # 이미지 열화 (블러·다운샘플·마스킹·crop·JPEG)
│   ├── ladders.py        # 텍스트 사다리 A(절단), B(변이체)
│   ├── encode.py         # 임베딩 추출 + 캐싱
│   ├── metrics.py        # L2M, RMG, Kendall-τ, 차원별 기여도
│   ├── grid.py           # 2차원 격자 실행 및 집계
│   ├── shift.py          # embedding shift (Phase 5)
│   └── eval.py           # retrieval, zero-shot classification
├── notebooks/            # 탐색적 분석, 시각화
├── results/
│   ├── embeddings/       # 캐시 — 반드시 사용
│   └── figures/
└── PROJECT.md            # 이 문서
```

**임베딩 캐싱은 선택이 아니다.** 격자 실험이라 같은 조합을 반복 계산하기 쉽다. 열화 방식·강도·텍스트 변이체를 키로 하는 캐시를 만들어라.

---

## 12. 원 논문과 맞출 상수

- 표본 수 n = 5,000
- 손실 평가용 batch size = 50
- 기준 온도 τ = 1/100 (비교: 1/50, 1)
- 시드 3개, 95% 신뢰구간
- shift 공식: `x_shift = Normalize(x - λ·Δ_gap)`, `y_shift = Normalize(y + λ·Δ_gap)` — 양쪽 반대 방향 동시 이동 후 재정규화

---

## 13. 참고 링크

### 논문

| | |
|---|---|
| CLIP | https://arxiv.org/abs/2103.00020 |
| Mind the Gap | https://openreview.net/pdf?id=S7Evzt9uit3 |
| Mind the Gap 코드 | https://github.com/Weixin-Liang/Modality-Gap |
| Mind the Gap 문서 | https://modalitygap.readthedocs.io/ |
| Two Effects One Trigger | https://arxiv.org/abs/2404.07983 |
| Two Effects 코드 | https://github.com/lmb-freiburg/two-effects-one-trigger |
| Two Effects 리뷰 | https://openreview.net/forum?id=uAFHCZRmXk |
| SemArt | https://noagarcia.github.io/docs/VISART2018.pdf |

### 데이터

| | |
|---|---|
| MS-COCO | https://cocodataset.org/ |
| SemArt 프로젝트 | https://noagarcia.github.io/SemArt/ |
| SemArt 다운로드 | https://doi.org/10.17036/researchdata.aston.ac.uk.00000380 |
| SemArt 코드 | https://github.com/noagarcia/SemArt |
| CUB-200-2011 | https://data.caltech.edu/records/65de6-vp158 |
| 미술 데이터셋 목록 | https://github.com/georgeblck/art-datasets |

### 도구

| | |
|---|---|
| OpenCLIP | https://github.com/mlfoundations/open_clip |
| Long-CLIP | https://github.com/beichenzbc/Long-CLIP |
| Long-CLIP 체크포인트 | https://huggingface.co/BeichenZhang/LongCLIP-L |
| Long-CLIP transformers | https://huggingface.co/creative-graphic-design/LongCLIP-B |
| CLIP_benchmark | https://github.com/LAION-AI/CLIP_benchmark |

---

## 14. 첫 주 체크리스트

- [ ] OpenCLIP 설치, ViT-B/16 `openai` 체크포인트 로드
- [ ] COCO validation 준비
- [ ] **Phase 0**: L2M = 0.82 재현
- [ ] 이미지 열화 함수 구현 (블러, 다운샘플, crop 최소 3종)
- [ ] **Phase 1**: COCO 1,000쌍으로 U자 곡선 파일럿
- [ ] **결과 보고 후 다음 단계 논의** ← 여기서 반드시 멈출 것
- [ ] (병행) SemArt 다운로드, 이미지 링크 생존율 확인
- [ ] (병행) SemArt 라이선스 문구 확인
- [ ] (병행) artistic comment 토큰 길이 히스토그램 (20/40/77 초과 비율)

**Phase 1 결과가 이 프로젝트의 방향을 결정한다. 거기까지 가서 멈추고 보고하라.**
